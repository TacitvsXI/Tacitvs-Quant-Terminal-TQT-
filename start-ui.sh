#!/bin/bash

echo "🎨 Starting TEZERAKT UI (Next.js 16)..."

# Переходим в корень проекта
cd "$(dirname "$0")"

# Переходим в директорию UI
cd apps/ui

# Запускаем Next.js
echo "✅ Starting Next.js on http://localhost:3000"
npm run dev















